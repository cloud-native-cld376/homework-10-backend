<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="site-header">
    <h1>{{title}}</h1>
    {{#if dbType}}
      <p class="meta">Backend: <code>{{dbType}}</code></p>
    {{/if}}
  </header>
  <main class="container">
    {{{body}}}
  </main>
</body>
</html>
